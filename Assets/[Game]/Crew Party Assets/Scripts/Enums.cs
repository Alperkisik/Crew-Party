public enum WeaponType
{
    Pillow, Chair, Bat, Rifle, Shotgun, Unarmed
}

public enum AttackType
{
    Melee, Range
}

public enum GateType
{
    Numerical, Weapon
}

public enum StickmanType
{
    Ally, Enemy
}